#include <stdio.h>
	void main()
		{
			int a;

			printf("Enter A:");
			scanf("%d",&a);

			if(a%2==0)
			{
				printf("%d is Even number",a);
			}

			else
			{
				printf("%d is odd number",a);
			}
		}